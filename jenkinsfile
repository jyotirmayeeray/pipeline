pipeline {
    agent any

    stages {
        stage('Git') {
            steps{
                git branch: 'master', url: 'https://github.com/jyotirmayeeray/pipeline.git'
            }
        }
        stagsh'bash shell.sh'
            steps{
                sh 'bash file.sh'
            }
        }
    }
}
